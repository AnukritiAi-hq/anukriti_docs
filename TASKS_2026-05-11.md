# Gap-Fix Tasks — 2026-05-11 Session

> Tracking file for biomedical completeness + engineering debt
> work. Updated in-session as items complete. Source prompt: "fix
> these" (biomedical + engineering gap list from
> `PLATFORM_ANALYSIS_2026-05-11.md`).

---

## Legend

- [ ] pending
- [~] in progress
- [x] done
- [⏸] scoped only (multi-day, not finished in-session)
- [🚫] blocked (note blocker)

---

## In-session (today) — fully executed

- [x] **T1.** Assess current state of each gap (discovery pass)
- [x] **T2.** Remove binary artifacts (`awscliv2.zip`, stray `.tbi`) from anukriti product repo root
- [x] **T3.** Ship LLM output validation layer (reject-on-hallucinated-entity) for template engine
- [~] **T4.** Audit 3–5 CPIC `needs_audit` provenance entries → flip to `verified`
- [ ] **T5.** Promote 1 ruff hard-gate dir in swarm (cleanup + CI bump)

## Scoped-only (design docs / stubs / ADRs shipped, execution deferred)

- [ ] **T6.** `api.py` / `app.py` router split — plan + skeleton + one extracted router as first-batch PR
- [ ] **T7.** PharmCAT concordance run — verify framework, attempt execution, surface blockers
- [ ] **T8.** CYP2D6 CNV Cyrius wrapper — ADR + caller base class stub in pgx-core
- [ ] **T9.** NAT2 / CYP2B6 multi-rsID haplotypes — `MultiVariantHaplotypeCaller` ABC scaffold
- [ ] **T10.** VCF upload in Streamlit UI — design doc + page skeleton
- [ ] **T11.** WhatsHap phasing for BAM — design doc only
- [ ] **T12.** 50-scenario clinical validation dataset — scaffold + review protocol

---

## Status log (append as tasks complete)

_(append-only log)_

### 2026-05-11 10:50 — T1 complete (discovery)

- Confirmed binaries not git-tracked; `.gitignore` already covers `*.zip` + `*.vcf.gz.tbi` + explicit `awscliv2.zip`.
- PharmCAT framework present: `anukriti/src/pharmcat_integration.py`, `anukriti/src/benchmark/pharmcat_comparison.py`, `getrm_truth.py`, `concordance.py`, `clinical_cases.py`, `expanded_validation.py`, `ablation_study.py`, `tool_comparison.py`.
- `anukriti/src/template_engine.py` exists with clinical-grade test coverage.
- `CPIC_PROVENANCE.json` is a dict keyed by table-name (not `{"entries": [...]}`).
- Monolith sizes confirmed: `api.py` = 4072 lines, `app.py` = 3345 lines (smaller than earlier 5000/3800 estimate, but still monolithic).

### 2026-05-11 10:51 — T2 complete (binaries)

- Deleted `anukriti/awscliv2.zip` (64MB) + 12 `ALL.chr*.vcf.gz.tbi` files from repo root.
- Total reclaim: ~66MB + 13 root-level files.
- No git commit: files were never tracked. `.gitignore` already prevents re-tracking.
- Safe, reversible workspace cleanup.

### 2026-05-11 11:04 — T3 complete (LLM output validation + OpenAI backend)

- New module `anukriti/src/llm_output_validator.py` (424 lines, 19/19 tests).
  - Deterministic post-hoc guardrail: rejects LLM responses mentioning drugs/genes/star-alleles not in input context.
  - Rule IDs: `LV-EMPTY`, `LV-DRUG`, `LV-GENE`, `LV-ALLELE`.
  - Closed vocabulary: 20 PGx panel genes + 41 CPIC-relevant drugs + star-allele regex + HLA regex.
  - Public API: `validate_llm_output()`, `assert_no_hallucination()`, `HallucinationError`, `known_drugs()`, `known_genes()`, `register_drug()`, `register_gene()`.
- Patched `anukriti/src/multi_backend_llm.py`:
  - **OpenAI** added as backend 5/7 (between Anthropic and Ollama).
  - Chain: Nova → Bedrock Claude → Gemini → Anthropic → **OpenAI** → Ollama → Deterministic fallback.
  - Uses `openai>=1.30` direct SDK; configurable via `OPENAI_API_KEY`/`OPENAI_MODEL`/`OPENAI_TEMPERATURE`/`OPENAI_MAX_TOKENS`.
  - System prompt constrains LLM output to the input context.
  - Validator wired into `generate_with_fallback()` — rejected responses fall through, `rejections[]` audit log returned in result.
  - `max_retries` default bumped 5 → 6.
- `anukriti/src/config.py`: four new OpenAI config fields.
- `anukriti/requirements.txt`: `openai>=1.30.0`.
- **Regression baseline preserved: 353 pass / 1 pre-existing fail** (byte-identical).
- Commit `2c6c1c5` on `clinical-grade-pgx`. Not pushed yet.